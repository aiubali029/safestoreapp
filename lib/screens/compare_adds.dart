import 'package:flutter/material.dart';

class CompareAdds extends StatefulWidget {
  const CompareAdds({Key? key}) : super(key: key);

  @override
  State<CompareAdds> createState() => _CompareAddsState();
}

class _CompareAddsState extends State<CompareAdds> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Compare Adds"),),);
  }
}
